# che-orm-macros

Procedural macros for [`che-orm`](https://crates.io/crates/che-orm).

Use this crate through `che-orm`; it is published separately only because
procedural macros require their own package.
